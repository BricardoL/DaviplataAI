import * as React from "react";
import { StyleSheet, View } from "react-native";

const Image1 = () => {
  return <View style={styles.image16} />;
};

const styles = StyleSheet.create({
  image16: {
    flex: 1,
    width: "100%",
    height: 1075,
  },
});

export default Image1;
